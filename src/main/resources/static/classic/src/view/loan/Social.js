Ext.define('MyApp.view.bank.Social', {
	extend: 'Ext.Container',
	xtype: 'social',
	autoDestroy: false,
	 
	initComponent: function () {
		Ext.apply(this, {
			 
			title: 'social',
			items: [ 
			]
		});
		this.callParent();
	}
});
